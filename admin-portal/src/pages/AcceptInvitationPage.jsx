import React, { useMemo, useState } from "react";
import { adminApiRequest } from "../lib/adminApi";
export default function AcceptInvitationPage(){ const token=useMemo(()=>new URLSearchParams(window.location.search).get("token")||"",[]); const [password,setPassword]=useState(""); const [name,setName]=useState(""); const [busy,setBusy]=useState(false); const [message,setMessage]=useState(""); async function submit(e){e.preventDefault();setBusy(true);setMessage("");try{await adminApiRequest("/staff/invitations/accept",{method:"POST",authRequired:false,body:{token,password,name}});setMessage("Account activated. You can now sign in.");}catch(err){setMessage(err.message)}finally{setBusy(false)}} return <div className="admin-login"><div className="admin-login__card"><div className="admin-login__brand">RNS INFOTECH</div><h1>Activate admin account</h1><p>Set your initial password to finish accepting the invitation.</p><form onSubmit={submit} className="admin-login__form"><label>Name<input className="admin-input" value={name} onChange={e=>setName(e.target.value)} placeholder="Optional"/></label><label>Password<input className="admin-input" type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="admin-btn admin-btn--primary" disabled={busy||!token}>{busy?"Activating…":"Activate account"}</button></form>{message&&<p>{message}</p>}</div></div> }
